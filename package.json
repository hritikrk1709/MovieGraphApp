{
  "name": "moviegraphapp",
  "private": true,
  "scripts": {
    "dev": "concurrently \"dotnet run\" \"npm --prefix frontend run dev\"",
    "build": "npm --prefix frontend run build",
    "start": "dotnet run"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
